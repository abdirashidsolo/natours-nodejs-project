doctype html 
html 
    head 
        block head
            link(rel='shortcut icon', type='image/png' href='/img/favicon.png' )
            link(rel='stylesheet' type='text/css' href='/css/style.css' )
            link(href='https://fonts.googleapis.com/css?family=Lato:300,300i,700' rel='stylesheet')    
            meta(charset='UTF-8') 
            meta(name='viewport' content='width=device-width, initial-scale=1.0') 
            title Natours | #{title} 
    
    body(data-alert=`${alert ? alert : ''}`)
    //HEADER
    include _header 

    // Content
    block content 
        h1 All tours goes here

     

    // FOOTER
    include _footer

    //- script(src="https://cdn.jsdelivr.net/npm/axios@1.3.3/dist/axios.min.js")
    //- script( src="/js/login.js" defer)
    //- script( src="/js/leaflet.js" defer)
    //- script( src="/js/index.js" defer)
    script(src='https://js.stripe.com/v3/' defer)
    script( src="/js/bundler.js" defer)
